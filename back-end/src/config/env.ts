// object to store the env keys/variables
export const env = {
  serverPort: process.env.SERVER_PORT,
  mongoUri: process.env.MONGO_URI,
  osrmUrl: "http://router.project-osrm.org/route/v1/driving/",
};
